apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: rob-demo
on:
  push:
    branches:
      - "**"
jobs:
  build:
    runs-on:
      - my-label-3
      - helloworld
    steps:
      - name: Say hello
        run: |
          echo "hello world"
        uses: docker://golang:1.20.3-alpine3.17
        shell: sh
